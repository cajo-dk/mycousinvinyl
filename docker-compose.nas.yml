
services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: mycousinvinyl-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: mycousinvinyl
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    volumes:
      - ${POSTGRES_DATA_DIR:-/volume1/DockerVolumes/mcv/postgres}:/var/lib/postgresql/data
      - ./infrastructure/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    # Port only accessible within Docker network (not exposed to host)
    expose:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - backend

  # ActiveMQ Message Broker
  activemq:
    image: apache/activemq-classic:6.1.3
    container_name: mycousinvinyl-activemq
    restart: unless-stopped
    # Ports only accessible within Docker network (not exposed to host)
    ports:
      - "8161:8161"
    expose:
      - "61616"  # OpenWire
      - "61613"  # STOMP
      - "8161"   # Admin UI
    environment:
      ACTIVEMQ_ADMIN_LOGIN: ${ACTIVEMQ_ADMIN_LOGIN:-admin}
      ACTIVEMQ_ADMIN_PASSWORD: ${ACTIVEMQ_ADMIN_PASSWORD:-admin}
    volumes:
      - activemq_data:/opt/apache-activemq/data
    healthcheck:
      test: ["CMD-SHELL", "test -f /opt/apache-activemq/data/activemq.pid"]
      interval: 10s
      timeout: 10s
      retries: 5
    networks:
      - backend

  # Backend API Service
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: api
    container_name: mycousinvinyl-api
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/mycousinvinyl
      MESSAGE_BROKER: ${MESSAGE_BROKER:-activemq}
      ACTIVEMQ_URL: stomp://activemq:61613
      MQTT_URL: ${MQTT_URL:-mqtt://mosquitto:1883}
      MQTT_USERNAME: ${MQTT_USERNAME:-}
      MQTT_PASSWORD: ${MQTT_PASSWORD:-}
      MQTT_TOPIC_PREFIX: ${MQTT_TOPIC_PREFIX:-mycousinvinyl}
      ACTIVITY_TOPIC: ${ACTIVITY_TOPIC:-/topic/system.activity}
      ACTIVITY_BRIDGE_TOKEN: ${ACTIVITY_BRIDGE_TOKEN:-}
      AZURE_TENANT_ID: ${AZURE_TENANT_ID}
      AZURE_CLIENT_ID: ${AZURE_CLIENT_ID}
      AZURE_AUDIENCE: ${AZURE_AUDIENCE}
      AZURE_GROUP_ADMIN: ${AZURE_GROUP_ADMIN:-}
      AZURE_GROUP_EDITOR: ${AZURE_GROUP_EDITOR:-}
      AZURE_GROUP_VIEWER: ${AZURE_GROUP_VIEWER:-}
      DISCOGS_SERVICE_URL: ${DISCOGS_SERVICE_URL:-http://discogs-service:8001}
      DISCOGS_CONSUMER_KEY: ${DISCOGS_CONSUMER_KEY:-}
      DISCOGS_CONSUMER_SECRET: ${DISCOGS_CONSUMER_SECRET:-}
      DISCOGS_OAUTH_CALLBACK_URL: ${DISCOGS_OAUTH_CALLBACK_URL:-}
      DISCOGS_OAUTH_AUTHORIZE_URL: ${DISCOGS_OAUTH_AUTHORIZE_URL:-}
      DISCOGS_OAUTH_API_BASE_URL: ${DISCOGS_OAUTH_API_BASE_URL:-}
      DISCOGS_OAUTH_RATE_LIMIT_PER_MINUTE: ${DISCOGS_OAUTH_RATE_LIMIT_PER_MINUTE:-55}
      DISCOGS_IMPORT_LOG_LEVEL: ${DISCOGS_IMPORT_LOG_LEVEL:-INFO}
      FRONTEND_BASE_URL: ${FRONTEND_BASE_URL:-https://ws01.cajo.dk}
      ALBUM_WIZARD_API_URL: ${ALBUM_WIZARD_API_URL:-}
      ALBUM_WIZARD_API_KEY: ${ALBUM_WIZARD_API_KEY:-}
      ALBUM_WIZARD_MODEL_ID: ${ALBUM_WIZARD_MODEL_ID:-}
      ALBUM_WIZARD_TIMEOUT_SECONDS: ${ALBUM_WIZARD_TIMEOUT_SECONDS:-30}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend:/app
    networks:
      - backend
      - frontend
    command: uvicorn app.entrypoints.http.main:app --host 0.0.0.0 --port 8000 --reload

  # Discogs Integration Service
  discogs-service:
    build:
      context: ./discogs-service
      dockerfile: Dockerfile
    container_name: mycousinvinyl-discogs
    restart: unless-stopped
    environment:
      DISCOGS_USER_AGENT: ${DISCOGS_USER_AGENT}
      DISCOGS_KEY: ${DISCOGS_KEY}
      DISCOGS_SECRET: ${DISCOGS_SECRET}
      DISCOGS_OAUTH_TOKEN: ${DISCOGS_OAUTH_TOKEN:-}
      DISCOGS_OAUTH_TOKEN_SECRET: ${DISCOGS_OAUTH_TOKEN_SECRET:-}
      DISCOGS_RATE_LIMIT_PER_MINUTE: ${DISCOGS_RATE_LIMIT_PER_MINUTE:-55}
      DISCOGS_IMPORT_LOG_LEVEL: ${DISCOGS_IMPORT_LOG_LEVEL:-INFO}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    expose:
      - "8001"
    networks:
      - backend

  # Outbox Processor Worker
  outbox-processor:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: worker
    container_name: mycousinvinyl-outbox-processor
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/mycousinvinyl
      MESSAGE_BROKER: ${MESSAGE_BROKER:-activemq}
      ACTIVEMQ_URL: stomp://activemq:61613
      MQTT_URL: ${MQTT_URL:-mqtt://mosquitto:1883}
      MQTT_USERNAME: ${MQTT_USERNAME:-}
      MQTT_PASSWORD: ${MQTT_PASSWORD:-}
      MQTT_TOPIC_PREFIX: ${MQTT_TOPIC_PREFIX:-mycousinvinyl}
      ACTIVITY_TOPIC: ${ACTIVITY_TOPIC:-/topic/system.activity}
      ACTIVITY_BRIDGE_URL: ${ACTIVITY_BRIDGE_URL:-http://api:8000}
      ACTIVITY_BRIDGE_TOKEN: ${ACTIVITY_BRIDGE_TOKEN:-}
      AZURE_TENANT_ID: ${AZURE_TENANT_ID}
      AZURE_CLIENT_ID: ${AZURE_CLIENT_ID}
      AZURE_AUDIENCE: ${AZURE_AUDIENCE}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend:/app
    networks:
      - backend
    command: python -m app.entrypoints.workers.outbox_processor

  # Event Consumer Worker
  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: worker
    container_name: mycousinvinyl-worker
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/mycousinvinyl
      MESSAGE_BROKER: ${MESSAGE_BROKER:-activemq}
      ACTIVEMQ_URL: stomp://activemq:61613
      MQTT_URL: ${MQTT_URL:-mqtt://mosquitto:1883}
      MQTT_USERNAME: ${MQTT_USERNAME:-}
      MQTT_PASSWORD: ${MQTT_PASSWORD:-}
      MQTT_TOPIC_PREFIX: ${MQTT_TOPIC_PREFIX:-mycousinvinyl}
      ACTIVITY_TOPIC: ${ACTIVITY_TOPIC:-/topic/system.activity}
      AZURE_TENANT_ID: ${AZURE_TENANT_ID}
      AZURE_CLIENT_ID: ${AZURE_CLIENT_ID}
      AZURE_AUDIENCE: ${AZURE_AUDIENCE}
      AZURE_GROUP_ADMIN: ${AZURE_GROUP_ADMIN:-}
      AZURE_GROUP_EDITOR: ${AZURE_GROUP_EDITOR:-}
      AZURE_GROUP_VIEWER: ${AZURE_GROUP_VIEWER:-}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend:/app
    networks:
      - backend
    command: python -m app.entrypoints.workers.consumer

  # Cache Cleanup Worker
  cache-cleanup:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: worker
    container_name: mycousinvinyl-cache-cleanup
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/mycousinvinyl
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend:/app
    networks:
      - backend
    command: python -m app.entrypoints.workers.cache_cleanup
    restart: unless-stopped

  # Pricing Worker (Marketplace Price Updates)
  pricing-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: worker
    container_name: mycousinvinyl-pricing-worker
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/mycousinvinyl
      MESSAGE_BROKER: ${MESSAGE_BROKER:-activemq}
      ACTIVEMQ_URL: stomp://activemq:61613
      MQTT_URL: ${MQTT_URL:-mqtt://mosquitto:1883}
      MQTT_USERNAME: ${MQTT_USERNAME:-}
      MQTT_PASSWORD: ${MQTT_PASSWORD:-}
      MQTT_TOPIC_PREFIX: ${MQTT_TOPIC_PREFIX:-mycousinvinyl}
      DISCOGS_SERVICE_URL: ${DISCOGS_SERVICE_URL:-http://discogs-service:8001}
      AZURE_TENANT_ID: ${AZURE_TENANT_ID}
      AZURE_CLIENT_ID: ${AZURE_CLIENT_ID}
      AZURE_AUDIENCE: ${AZURE_AUDIENCE}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    depends_on:
      postgres:
        condition: service_healthy
      discogs-service:
        condition: service_started
    volumes:
      - ./backend:/app
    networks:
      - backend
    command: python -m app.entrypoints.workers.pricing_worker
    restart: unless-stopped

  activity-bridge:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: worker
    container_name: mycousinvinyl-activity-bridge
    restart: unless-stopped
    environment:
      MESSAGE_BROKER: ${MESSAGE_BROKER:-activemq}
      ACTIVEMQ_URL: stomp://activemq:61613
      MQTT_URL: ${MQTT_URL:-mqtt://mosquitto:1883}
      MQTT_USERNAME: ${MQTT_USERNAME:-}
      MQTT_PASSWORD: ${MQTT_PASSWORD:-}
      MQTT_TOPIC_PREFIX: ${MQTT_TOPIC_PREFIX:-mycousinvinyl}
      ACTIVITY_TOPIC: ${ACTIVITY_TOPIC:-/topic/system.activity}
      ACTIVITY_BRIDGE_URL: ${ACTIVITY_BRIDGE_URL:-http://api:8000}
      ACTIVITY_BRIDGE_TOKEN: ${ACTIVITY_BRIDGE_TOKEN:-}
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/mycousinvinyl
      AZURE_TENANT_ID: ${AZURE_TENANT_ID}
      AZURE_CLIENT_ID: ${AZURE_CLIENT_ID}
      AZURE_AUDIENCE: ${AZURE_AUDIENCE}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    depends_on:
      api:
        condition: service_started
    networks:
      - backend
      - frontend
    command: python -m app.entrypoints.workers.activity_ws_bridge


  # Frontend SPA
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-https://ws01.cajo.dk}
        VITE_AZURE_CLIENT_ID: ${AZURE_CLIENT_ID}
        VITE_AZURE_TENANT_ID: ${AZURE_TENANT_ID}
        VITE_AZURE_REDIRECT_URI: ${VITE_AZURE_REDIRECT_URI:-https://ws01.cajo.dk}
        VITE_AZURE_GROUP_ADMIN: ${AZURE_GROUP_ADMIN:-}
        VITE_MANIFEST_ENV: ${VITE_MANIFEST_ENV:-nas}
    container_name: mycousinvinyl-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - api
    networks:
      - frontend

networks:
  backend:
    driver: bridge
  frontend:
    driver: bridge

volumes:
  postgres_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${POSTGRES_DATA_PATH:-/volume1/docker/mycousinvinyl/postgres}
  activemq_data:
